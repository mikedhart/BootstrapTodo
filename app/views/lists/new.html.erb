<h1>New list</h1>

<%= content_tag :div, class: "wrap" do %>
	<%= form_for @list do |f| %>
		<%= f.text_field :name, autocomplete: false, placeholder: "Enter list name here" %>
		<%= f.submit %>
	<% end %>
<% end %>

<%= link_to 'Back', lists_path %>
