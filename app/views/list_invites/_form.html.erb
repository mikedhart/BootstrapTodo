<%= form_for(@list_invite) do |f| %>
  <% if @list_invite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list_invite.errors.count, "error") %> prohibited this list_invite from being saved:</h2>

      <ul>
      <% @list_invite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.hidden_field :list_id %>
    <%= f.submit %>
  </div>
<% end %>
