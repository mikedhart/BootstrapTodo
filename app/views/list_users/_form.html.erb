<%= form_for(@list_user) do |f| %>
  <% if @list_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list_user.errors.count, "error") %> prohibited this list_user from being saved:</h2>

      <ul>
      <% @list_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :list_id %><br>
    <%= f.number_field :list_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
